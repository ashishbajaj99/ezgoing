var express = require('express');
var router = express.Router();
//var powerCalc = require('power-calculator');

/* GET users listing. */
router.get('/', function(req, res) {
  //  res.json(powerCalc());
});

module.exports = router;
